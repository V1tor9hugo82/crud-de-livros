<div id="book-page">
  <a href="/books">Voltar</a>
  <div class="container">
    <h1>{{this.title}}</h1>
    <p>Este livro tem {{book.pagyqty}} paginas.</p>
    <div class="actions">
      <a href="/books/edit/{{book.id}}">Editar</a>
      <form action="/books/remove/{{book.id}}" method="POST">
        <input type="submit" value="Remover">
      </form>
    </div>
  </div>
</div>